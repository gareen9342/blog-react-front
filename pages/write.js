import React from 'react'
import PropTypes from 'prop-types'
import AppLayout from '../components/AppLayout'

const write = (props) => {
    return <AppLayout>write</AppLayout>
}

write.propTypes = {}

export default write
