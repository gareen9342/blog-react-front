import React from 'react'
import PropTypes from 'prop-types'
import AppLayout from '../../components/AppLayout'

const Posts = (props) => {
    return <AppLayout>posts</AppLayout>
}

Posts.propTypes = {}

export default Posts
